import React from 'react';
import UpdateMember from './UpdateMember.jsx';

export default {
  title: 'Molecules/UpdateMember',
  component: UpdateMember,
  tags: ['autodocs'],
};

const Template = (args) => <UpdateMember {...args} />;

export const Default = Template.bind({});
